listings[['city']]
# how many of the listings are in Columbus?
listings[listings$city == "Columbus"]
# how many of the listings are in Columbus?
listings[listings$city == "Columbus", ]
# how many of the listings are in Columbus?
Columbus <- listings[listings$city == "Columbus", ]
Chicago <- listings[listings$city == "Chicago"]
Chicago <- listings[listings$city == "Chicago", ]
Chicago[which.max(Chicago$price), ]
# how many distinct values in the neighbourhood column among the chicago listings
unique(Chicago$neighbourhood)
# how many distinct values in the neighbourhood column among the chicago listings
length(unique(Chicago$neighbourhood))
# how many listings are run by a superhost
listings$host_is_superhost == 't'
# how many listings are run by a superhost
sum(listings$host_is_superhost == 't', na.rm = TRUE)
# how many listings are run by a superhost
sum(listings$host_is_superhost, na.rm = TRUE)
# how many listings are run by a superhost
sum(listings$host_is_superhost == 't', na.rm = TRUE)
# how many listings cost more than $1000
listings[listings$price > 1000]
# how many listings cost more than $1000
listings[listings$price > 1000, ]
# how many listings cost more than $1000
listings$price > 1000
# how many listings cost more than $1000
sum(listings$price > 1000, na.rm = TRUE)
DBI::dbReadTable(con, "listings")
knitr::opts_chunk$set(echo = TRUE)
run = readr::read_csv(
"https://isa401-find-your-seat.vercel.app/api/export?section=A&run=13")
dplyr::glimpse(run)
listings = DBI::dbReadTable(con, "listings")
install.packages("RSQLite")
con = DBI::dbConnect(RSQLite::SQLite(), "../data/midwest_airbnb.db")
listings = DBI::dbReadTable(con, "listings")
View(run)
install.packages('querychat')
install.packages('usethis')
usethis::edit_r_environ(scope = "project")
setwd("M:/business_intelligence/apps/job_scout_chat")
con = DBI::dbConnect(RSQLite::SQLite(), "data/scout.db")
qc = querychat::querychat(
con, "scout_postings",
client   = client,
tools    = c("filter", "query", "visualize"),
greeting = "Ask me about the 1,891 job postings
ChatISA Job Scout collected."
)
client = ellmer::chat_openai(
model = "gpt-5.6-luna",
params = ellmer::params(reasoning_effort = "none")
)
qc = querychat::querychat(
con, "scout_postings",
client   = client,
tools    = c("filter", "query", "visualize"),
greeting = "Ask me about the 1,891 job postings
ChatISA Job Scout collected."
)
install.packages("ggsql")
qc = querychat::querychat(
con, "scout_postings",
client   = client,
tools    = c("filter", "query", "visualize"),
greeting = "Ask me about the 1,891 job postings
ChatISA Job Scout collected."
)
qc$app()
usethis::edit_r_environ("project")
setwd("M:/business_intelligence/apps/job_scout_chat")
con = DBI::dbConnect(RSQLite::SQLite(), "data/scout.db")
client = ellmer::chat_openai(
model = "gpt-5.6-luna",
params = ellmer::params(reasoning_effort = "none")
)
qc = querychat::querychat(
con, "scout_postings",
client   = client,
tools    = c("filter", "query", "visualize"),
greeting = "Ask me about the 1,891 job postings
ChatISA Job Scout collected."
)
qc$app()
setwd("M:/business_intelligence/markdown")
knitr::opts_chunk$set(echo = TRUE)
con = DBI::dbConnect(RSQLite::SQLite(), "../data/scout.db")
postings = DBI::dbReadTable(con, "scout_postings") |> tibble::as_tibble()
con = DBI::dbConnect(RSQLite::SQLite(), "../data/scout.db")
postings = DBI::dbReadTable(con, "scout_postings") |> tibble::as_tibble()
DBI::dbDisconnect(con)
jobs = postings |>
dplyr::select(company, title, location_state, remote, category, posted_at, last_seen_at) |>
dplyr::mutate(dplyr::across(dplyr::ends_with("_at"), as.Date),
days_open = as.numeric(last_seen_at - posted_at))
dplyr::glimpse(postings)
dplyr::glimpse(jobs)
jobs |>
dplyr::group_by(category) |>
dplyr::summarise(
postings = dplyr::n()
)
jobs |>
dplyr::group_by(category) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote)
)
jobs |>
dplyr::group_by(category) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
)
# Q2: Among the fulltime jobs, which states have the highest remote share?
jobs |>
dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::arrange(dplyr::desc(remote_share))
# Q2: Among the fulltime jobs, which states have the highest remote share?
jobs |>
dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::filter(postings >= 50)
# Q2: Among the fulltime jobs, which states have the highest remote share?
jobs |>
dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::filter(postings >= 50) |> # up to you to determine the min threshold
dplyr::arrange(dplyr::desc(remote_share))
# Q2: Among the fulltime jobs, which states have the highest remote share?
jobs |>
dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::filter(postings >= 50) |> # up to you to determine the min threshold
dplyr::arrange(dplyr::desc(remote_share)) |>
head(3)
jobs |>
# dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state, category) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::filter(postings >= 50) |> # up to you to determine the min threshold
dplyr::arrange(dplyr::desc(remote_share)) |>
head(3)
jobs |>
# dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state, category) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::filter(postings >= 50) |> # up to you to determine the min threshold
```
jobs |>
# dplyr::filter(category == 'fulltime') |>
dplyr::group_by(location_state, category) |>
dplyr::summarise(
postings = dplyr::n(), # counting the number of rows per category
remote_share = mean(remote) |> round(3)
) |>
dplyr::filter(postings >= 50) # up to you to determine the min threshold
con = DBI::dbConnect(RSQLite::SQLite(), "../data/midwest_airbnb.db")
listings = DBI::dbReadTable(con, "listings") |>
tibble::as_tibble()
DBI::dbDisconnect(con)
dplyr::glimpse(listings)
View(listings)
listings |>
dplyr::group_by(city)
listings |>
dplyr::group_by(city) |>
dplyr::summarise(dplyr::n())
install.packages("httr2")
setwd("M:/business_intelligence/markdown")
knitr::opts_chunk$set(echo = TRUE)
con = DBI::dbConnect(RSQLite::SQLite(), "../data/midwest_airbnb.db")
listings = DBI::dbReadTable(con, "listings") |>
tibble::as_tibble()
DBI::dbDisconnect(con)
dplyr::glimpse(listings)
# Q1: Approach a) would be to simply count the repetitions within the city col
table(listings$city)
listings |>
dplyr::group_by(city) |>
dplyr::summarise(
city_count = dplyr::n()
) -> practice_q1
practice_q1
# Q2: what is the median price in each city, listings that sleep 4 or more
listings |>
dplyr::filter(
room_type == "Entire home/apt" & accommodates >= 4
)
# Q2: what is the median price in each city, listings that sleep 4 or more
listings |>
dplyr::filter(
room_type == "Entire home/apt" & accommodates >= 4
) |>
dplyr::group_by(city) |>
dplyr::summarise(
median_price = median(price)
)
listings |>
dplyr::mutate(
price_per_person = price / accomodates # LHS = new col name; RHS is the math used to create the new col [this is done by row]
)
listings |>
dplyr::mutate(
price_per_person = price / accommodates # LHS = new col name; RHS is the math used to create the new col [this is done by row]
)
listings |>
dplyr::mutate(
price_per_person = price / accommodates # LHS = new col name; RHS is the math used to create the new col [this is done by row]
) |>
dplyr::arrange(price_per_person) |>
# you do not need to do this step, just doing so we can look at the output in a more compact way
dplyr::select(city, name, price, accommodates, price_per_person)
listings |>
dplyr::mutate(
price_per_person = price / accommodates # LHS = new col name; RHS is the math used to create the new col [this is done by row]
) |>
dplyr::arrange(price_per_person) |>
# you do not need to do this step, just doing so we can look at the output in a more compact way
dplyr::select(city, name, price, accommodates, price_per_person) |>
head(3)
listings |>
dplyr::mutate(
price_per_person = price / accommodates # LHS = new col name; RHS is the math used to create the new col [this is done by row]
) |>
dplyr::arrange(price_per_person) |>
# you do not need to do this step, just doing so we can look at the output in a more compact way
dplyr::select(city, name, price, accommodates, price_per_person) |>
tail(3)
listings |>
dplyr::mutate(
price_per_person = price / accommodates # LHS = new col name; RHS is the math used to create the new col [this is done by row]
) |>
dplyr::arrange(price_per_person) |>
# you do not need to do this step, just doing so we can look at the output in a more compact way
dplyr::select(city, name, price, accommodates, price_per_person) |>
head(3)
# Q4: which 5 host names have the most listings?
listings |>
dplyr::count(host_name, sort = T) # short cut for group_by and then n()
# Q4: which 5 host names have the most listings?
listings |>
dplyr::count(host_name, host_id, sort = T) # short cut for group_by and then n()
knitr::opts_chunk$set(echo = TRUE)
httr2::request("https://data.cincinnati-oh.gov/resource/rvmt-pkmq.json")
httr2::request("https://data.cincinnati-oh.gov/resource/rvmt-pkmq.json") |>
httr2::req_url_query("$limit" = 5)
httr2::request("https://data.cincinnati-oh.gov/resource/rvmt-pkmq.json") |>
httr2::req_url_query("$limit" = 5) |>
httr2::req_perform()
httr2::request("https://data.cincinnati-oh.gov/resource/rvmt-pkmq.json") |>
httr2::req_url_query("$limit" = 5) |>
httr2::req_perform() |>
httr2::resp_body_string()
httr2::request("https://data.cincinnati-oh.gov/resource/rvmt-pkmq.json") |>
httr2::req_url_query("$limit" = 5) |>
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON()
usethis::edit_r_environment("project")
usethis::edit_r_environ("project")
knitr::opts_chunk$set(echo = TRUE)
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json"
) |>
# nothing has been sent yet
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON()
qc$app_obj()
library(querychat)
library(querychat)
# ISA 401 Job Scout Chat: ask questions, get SQL, a table, or a chart back
library(querychat)
con = DBI::dbConnect(RSQLite::SQLite(), "data/midwest_airbnb.db")
client = ellmer::chat_openai(
model  = "gpt-5.6-luna",
params = ellmer::params(reasoning_effort = "none")
)
qc = querychat(
con, "listings",
client   = client,
tools    = c("filter", "query", "visualize"),  # visualize: charts in the chat (needs ggsql)
greeting = "Ask me about 14,887 Airbnb listings in Chicago, Columbus, and the Twin Cities."
)
qc$app_obj()
knitr::opts_chunk$set(echo = TRUE)
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json"
)
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json"
) |>
# nothing has been sent yet
httr2::req_perform()
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json"
) |>
# nothing has been sent yet
httr2::req_perform() |>
httr2::resp_body_string()
ohio_jobs =
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json"
) |>
# nothing has been sent yet
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON()
dplyr::glimpse(ohio_jobs)
ohio_jobs[13]
ohio_df = ohio_jobs$observations
ohio_jobs =
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json",
observation_start = '2026-09-01'
) |>
# nothing has been sent yet
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON()
ohio_jobs =
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json",
observation_start = '2026-09-01'
) |>
# nothing has been sent yet
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON()
ohio_df = ohio_jobs$observations # we saw that the data is in observations
ohio_jobs =
httr2::request("https://api.stlouisfed.org/fred/series/observations") |>
httr2::req_url_query(
series_id = "IHLIDXUSOH",
api_key = Sys.getenv("FRED_API_KEY"),
file_type = "json"
) |>
# nothing has been sent yet
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON()
ohio_df = ohio_jobs$observations # we saw that the data is in observations
# another approach to filter the dates would be:
ohio_df |>
dplyr::mutate(date = lubridate::ymd(date))
# another approach to filter the dates would be:
ohio_df |>
# overwrite the date col and convert to date (from chr)
dplyr::mutate(date = lubridate::ymd(date)) |>
# because it is a date, we can do logical checks on it and it keeps dates from sep 1 2026
dplyr::filter(date >= '2026-09-01') -> ohio_df_sept_2026
knitr::opts_chunk$set(echo = TRUE)
httr2::request("https://data.usajobs.gov/api/search") |>
httr2::req_url_query(Keyword = "Data Analyst", ResultsPerPage = 500) |>
httr2::req_headers(
Host = "data.usajobs.gov",
"User-Agent" = "papandaj@miamioh.edu",
"Authorization-Key" = Sys.getenv("USA_API_KEY")
) |>
httr2::req_perform()
httr2::request("https://data.usajobs.gov/api/search") |>
httr2::req_url_query(Keyword = "Data Analyst", ResultsPerPage = 500) |>
httr2::req_headers(
Host = "data.usajobs.gov",
"User-Agent" = "papandaj@miamioh.edu",
"Authorization-Key" = Sys.getenv("USA_API_KEY")
) |>
httr2::req_perform()
rlang::last_trace()
httr2::request("https://data.usajobs.gov/api/search") |>
httr2::req_url_query(Keyword = "Data Analyst", ResultsPerPage = 500) |>
httr2::req_headers(
Host = "data.usajobs.gov",
"User-Agent" = "papandaj@miamioh.edu",
"Authorization-Key" = Sys.getenv("USA_API_KEY")
) |>
httr2::req_perform()
knitr::opts_chunk$set(echo = TRUE)
httr2::request("https://data.usajobs.gov/api/search") |>
httr2::req_url_query(Keyword = "Data Analyst", ResultsPerPage = 500) |>
httr2::req_headers(
Host = "data.usajobs.gov",
"User-Agent" = "papandaj@miamioh.edu",
"Authorization-Key" = Sys.getenv("USA_API_KEY")
) |>
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON(flatten = TRUE) -> usa_jobs_data
httr2::request("https://data.usajobs.gov/api/search") |>
httr2::req_url_query(Keyword = "Data Analyst", ResultsPerPage = 500) |>
httr2::req_headers(
Host = "data.usajobs.gov",
"User-Agent" = "papandaj@miamioh.edu",
"Authorization-Key" = Sys.getenv("USA_API_KEY")
) |>
httr2::req_perform() |>
httr2::resp_body_string() |>
jsonlite::fromJSON(flatten = TRUE) -> usa_jobs_data
usa_jobs_df = usa_jobs_data$SearchResult$SearchResultItems
jobs_small = usa_jobs_df |>
dplyr::select(
title = MatchedObjectDescriptor.PositionTitle,
department = MatchedObjectDescriptor.DepartmentName,
location = MatchedObjectDescriptor.PositionLocationDisplay
)
View(jobs_small)
jobs_small = usa_jobs_df |>
dplyr::select(
title = MatchedObjectDescriptor.PositionTitle,
department = MatchedObjectDescriptor.DepartmentName,
location = MatchedObjectDescriptor.PositionLocationDisplay,
closes = MatchedObjectDescriptor.ApplicationCloseDate
)
jobs_small = usa_jobs_df |>
dplyr::select(
title = MatchedObjectDescriptor.PositionTitle,
department = MatchedObjectDescriptor.DepartmentName,
agency = MatchedObjectDescriptor.OrganizationName
location = MatchedObjectDescriptor.PositionLocationDisplay,
jobs_small = usa_jobs_df |>
dplyr::select(
title = MatchedObjectDescriptor.PositionTitle,
department = MatchedObjectDescriptor.DepartmentName,
agency = MatchedObjectDescriptor.OrganizationName,
location = MatchedObjectDescriptor.PositionLocationDisplay,
closes = MatchedObjectDescriptor.ApplicationCloseDate
)
nrow(jobs_small)
table(jobs_small$department) |> sort(decreasing = T)
jobs_small |> dplyr::count(department) |>
dplyr::arrange(dplyr::desc(n)) |> head(3)
## the harder way but being literal
jobs_small |> dplyr::filter(title == 'Data Analyst')
index_data_analyst = jobs_small$title |>
stringr::str_detect("'Data Analyst' | 'Data Analytics'")
## the harder way without being literal
index_data_analyst = jobs_small$title |>
stringr::str_detect("Data Analy*")
## the harder way without being literal
index_data_analyst = jobs_small$title |>
stringr::str_detect("Data Analytics|Data Analyst")
#given that this is a true or false type result
# true rows will be kept
# , nothing means keep all col
count_standard = jobs_small[index_data_analyst , ]
## the harder way but being literal
jobs_small |> dplyr::filter(title == 'Data Analyst') -> count_lazy
## the harder way but being literal
jobs_small |> dplyr::filter(title == 'Data Analyst') -> count_literal
View(count_standard)
# Midwest AirBnb Chat: ask questions, get SQL, a table, or a chart back
library(querychat)
con = DBI::dbConnect(RSQLite::SQLite(), "data/midwest_airbnb.db")
client = ellmer::chat_openai(
model  = "gpt-5.6-luna",
params = ellmer::params(reasoning_effort = "none")
)
qc = querychat(
con, "listings",
client   = client,
tools    = c("filter", "query", "visualize"),  # visualize: charts in the chat (needs ggsql)
greeting = "Ask me about 14,887 Airbnb listings in Chicago, Columbus, and the Twin Cities."
)
qc$app_obj()
